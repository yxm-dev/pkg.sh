`pkg.sh` is a simple package builder written in pure shell and aimed to provide standardization to pure bash
projects, with configurations prescribed in a `pkgfile`.

The packages created with `pkg.sh` comes with `install.sh` and `unistall.sh` scripts that install/uninstall
the needed dependencies (defined in the `pkgfile`). The uninstall fully remove the package.


